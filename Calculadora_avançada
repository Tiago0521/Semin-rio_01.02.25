<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Calculadora Avançada</title>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/mathjs/10.6.4/math.min.js"></script>
    <style>
        body {
            font-family: Arial, sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            background-color: #f4f4f4;
        }
        .calculator {
            background: #fff;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
            text-align: center;
        }
        input {
            width: 100%;
            padding: 10px;
            font-size: 1.2rem;
            margin-bottom: 10px;
        }
        button {
            padding: 10px 20px;
            font-size: 1rem;
            margin: 5px;
            border: none;
            border-radius: 5px;
            background: #007bff;
            color: #fff;
            cursor: pointer;
            transition: background 0.3s;
        }
        button:hover {
            background: #0056b3;
        }
    </style>
</head>
<body>
    <div class="calculator">
        <h1>Calculadora Avançada</h1>
        <input type="text" id="expression" placeholder="Digite a expressão em inglês">
        <button onclick="calculate()">Calcular</button>
        <button onclick="clearInput()">Limpar</button>
        <h2>Resultado: <span id="result"></span></h2>
        <h3>Exemplos de uso:</h3>
        <p>sin(30), cos(45), tan(60), sqrt(16), log(10), exp(2), mean([1,2,3,4,5])</p>
    </div>

    <script>
        function calculate() {
            try {
                let expression = document.getElementById("expression").value;
                let result = math.evaluate(expression);
                document.getElementById("result").innerText = result;
            } catch (error) {
                document.getElementById("result").innerText = "Erro! Expressão inválida.";
            }
        }

        function clearInput() {
            document.getElementById("expression").value = "";
            document.getElementById("result").innerText = "";
        }
    </script>
</body>
</html>
